"""Reinforcement learning for the ``rl`` representation (RLlib PPO, agentic framework).

``spaces`` and ``policy`` import only NumPy; ``models``, ``rllib_env``, and
``training`` need the rl extra and are imported only for training or loading.
"""
